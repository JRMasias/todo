class Task {
          description;

          constructor(desc) {
                    this.description = desc;
          }

          getDescription() {
                    return this.description;
          }
}